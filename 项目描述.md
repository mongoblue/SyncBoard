# SyncBoard 项目描述

## 📋 项目概述

**SyncBoard** 是一个现代化的项目协作看板系统，基于 Django + Vue3 构建，支持实时同步、AI 助手、质量中心等功能。

## 🏗️ 项目架构

### 后端技术栈
| 技术 | 用途 |
|------|------|
| Django 6.0 + DRF | Web 框架与 REST API |
| Django Channels | WebSocket 实时通信 |
| Celery + Redis | 异步任务队列 |
| MySQL | 主数据库 |
| Elasticsearch + Haystack | 全文搜索引擎 |
| Playwright + requests | E2E 测试与接口测试 |
| Locust | 性能/压力测试 |

### 前端技术栈
| 技术 | 用途 |
|------|------|
| Vue 3 + TypeScript | 前端框架 |
| Element Plus | UI 组件库 |
| Pinia | 状态管理 |
| Vue Router | 路由管理 |
| ECharts | 数据可视化 |
| vuedraggable | 拖拽排序组件 |

## 📁 目录结构

```
SyncBoard/
├── backend/                    # Django 后端
│   ├── backend/              # 项目配置 (asgi, wsgi, settings)
│   ├── qa_center/           # 质量中心 (API/UI/性能测试)
│   │   ├── models.py        # 测试模型
│   │   ├── views*.py        # API视图
│   │   ├── serializers.py   # 序列化器
│   │   ├── urls.py          # 路由配置
│   │   └── api_auto_executor.py  # 接口自动化执行器
│   ├── room/                # 看板核心业务
│   ├── system/              # 系统管理 (用户/角色/菜单)
│   └── e2e/                 # E2E 测试用例
│
├── frontend/                  # Vue3 前端
│   ├── src/
│   │   ├── views/          # 页面组件
│   │   │   ├── qa/        # 质量中心页面
│   │   │   ├── system/     # 系统管理页面
│   │   │   └── Board.vue   # 看板主页面
│   │   ├── stores/         # Pinia 状态管理
│   │   ├── components/     # 公共组件
│   │   └── router/         # 路由配置
│   └── package.json
│
└── api_auto_test_skeleton/   # 独立接口自动化测试项目 (pytest)
    ├── common/             # 公共封装 (request, yaml, assertion, logger)
    ├── data/               # 测试数据 (YAML)
    ├── testcases/         # 测试用例
    ├── pytest.ini         # pytest 配置
    └── run.py             # 运行入口
```

## ✨ 核心功能

### 1. 看板管理
- 多项目管理，独立看板
- 自定义列（To Do / In Progress / Done）
- 任务卡片（标题、描述、负责人、标签、截止日期）
- 拖拽排序，批量操作

### 2. 实时同步
- WebSocket 实时更新
- 全局通知广播
- 在线用户状态

### 3. AI 助手
- RAG 智能问答（基于项目任务数据）
- DeepSeek API 集成
- 上下文感知的任务推荐

### 4. 质量中心
| 功能 | 说明 |
|------|------|
| **接口测试** | pytest + requests，YAML 配置测试数据 |
| **UI 测试** | Playwright 录制与回放，稳定的 role-based 选择器 |
| **性能测试** | Locust 压力测试，Web 界面配置 |
| **数据工厂** | Faker 生成测试数据 |
| **测试结果** | 持久化保存，截图与日志 |

### 5. 系统管理
- 用户注册/登录，头像上传
- RBAC 权限控制
- 动态菜单配置
- 系统通知推送

### 6. 搜索功能
- Elasticsearch + Haystack 全文搜索
- 中文 Ngram 分词
- 搜索结果实时高亮

## 🚀 快速启动

### 后端
```bash
cd backend
uvicorn backend.asgi:application --host 0.0.0.0 --port 8000 --reload
```

### 前端
```bash
cd frontend
npm install
npm run dev
```

## 🔌 主要 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/qa/auto-suites/` | GET/POST | API 自动化测试套件 |
| `/api/qa/auto-cases/` | GET/POST | API 自动化测试用例 |
| `/api/qa/auto-assertions/` | GET/POST | 测试断言配置 |
| `/api/qa/auto-results/` | GET | 测试结果查询 |
| `/api/qa/auto-execute/` | POST | 执行测试套件 |

## 📊 测试模块

### 接口自动化测试 (Django 内置)
- 支持状态码断言、JSON 字段值断言、字段存在断言
- 测试数据存储在 MySQL
- 提供完整的 CRUD API

### 独立测试项目 (pytest)
位于 `api_auto_test_skeleton/`，使用方式：
```bash
cd api_auto_test_skeleton
python run.py all        # 运行所有测试
python run.py smoke      # 冒烟测试
python run.py report    # 生成 Allure 报告
```
