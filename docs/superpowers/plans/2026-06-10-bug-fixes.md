# Bug 修复 + 质量报告语义修正 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 4 个 bug：成员管理改角色 UI 不刷新、看板筛选未联动成员、Bug 列表空数据、质量报告 Bug 密度语义错误。

**Architecture:**
- Bug 1/2 仅前端改动（最小修复）
- Bug 3 后端 + 前端：新增共享 seed 模块、management command、API 端点；前端空态按钮
- Bug 4 仅后端：把 `views_devops.py` 中的 `Tag` 统计替换为 `bug_tracker.Bug` 查询

**Tech Stack:**
- 后端：Django 4.x + DRF + pytest
- 前端：Vue 3 Composition API + Element Plus + Pinia
- 测试：pytest-django (--reuse-db --nomigrations)

---

## File Structure

### 新建文件
- `backend/bug_tracker/seed.py` — 共享 seed 逻辑（command + view 复用）
- `backend/bug_tracker/management/__init__.py` — 空
- `backend/bug_tracker/management/commands/__init__.py` — 空
- `backend/bug_tracker/management/commands/seed_bugs_demo.py` — `python manage.py seed_bugs_demo` 命令
- `backend/tests/test_bug_seed.py` — seed 幂等性 + 端点鉴权测试
- `backend/tests/test_quality_report.py` — 质量报告 bug 密度回归测试

### 修改文件
- `frontend/src/views/Members.vue` — changeRole 成功后 fetchMembers
- `frontend/src/views/Board.vue` — 新增 `projectMembers` computed
- `frontend/src/views/bug/BugList.vue` — 空态加 owner-only 按钮
- `frontend/src/api/bug.ts` — 新增 `seedDemoBugs` API
- `backend/bug_tracker/views.py` — 新增 `BugSeedDemoView`
- `backend/bug_tracker/urls.py` — 注册 `/bugs/seed-demo/`
- `backend/room/management/commands/seed_ci_data.py` — 串接 `seed_bugs_demo`
- `backend/qa_center/views_devops.py` — Bug 密度改用 `bug_tracker.Bug`

---

## Task 1: 修复成员管理改角色 UI 不刷新

**Files:**
- Modify: `frontend/src/views/Members.vue:209-218`

- [ ] **Step 1: 修改 changeRole 函数**

把 `frontend/src/views/Members.vue` 中 209-218 行的 `changeRole` 替换为：

```typescript
const changeRole = async (member: any, roleId: number) => {
  try {
    const userId = member.user_detail?.id || member.user?.id;
    await service.put(`/projects/${projectId.value}/members/`, {
      user_id: userId,
      role_id: roleId,
    });
    ElMessage.success('角色已更新');
    await fetchMembers();
  } catch (e: any) {
    ElMessage.error(e?.response?.data?.detail || '更新失败');
  }
};
```

- [ ] **Step 2: 手动验证**

启动 dev server（后端 + 前端）：
- `cd backend && python manage.py runserver`
- `cd frontend && npm run dev`

操作步骤：
1. 登录项目负责人账号
2. 进入 Members 页面
3. 对某成员切换 role 下拉
4. 期望：el-select 立即显示新角色名

期望输出：UI 立刻刷新；如果后端失败则弹出 `e.response.data.detail` 文本（不是笼统"更新失败"）。

- [ ] **Step 3: 提交**

```bash
cd D:/Projects/SyncBoard
git add frontend/src/views/Members.vue
git commit -m "fix(members): refresh list after role change; surface backend errors"
```

---

## Task 2: 看板筛选负责人联动项目成员

**Files:**
- Modify: `frontend/src/views/Board.vue`

- [ ] **Step 1: 在 Board.vue 中新增 projectMembers 计算属性**

在 `Board.vue` 的 `<script setup>` 顶部 import 之后（约 175 行附近，紧跟现有 ref/computed），新增：

```typescript
const projectMembers = computed(() => {
  const owner = boardStore.currentProject?.owner_details;
  const members = (boardStore.currentProject?.members_details || []).map((u: any) => ({
    id: u.id,
    username: u.username,
    profile: u.profile,
  }));
  const all = owner ? [{ id: owner.id, username: owner.username, profile: owner.profile }, ...members] : members;
  const seen = new Set<number>();
  return all.filter((u) => u && u.id != null && !seen.has(u.id) && seen.add(u.id));
});
```

- [ ] **Step 2: 把"筛选负责人" el-option 切换到 projectMembers**

`Board.vue` 中第 25-30 行：

```vue
<el-option
  v-for="user in projectMembers"
  :key="user.id"
  :label="user.username"
  :value="user.id"
/>
```

- [ ] **Step 3: 把"新建任务"指派人下拉切换到 projectMembers**

定位 `Board.vue` 中第 268 行 `el-select v-model="newTaskForm.assignee"`，将其内部 `el-option` 的 `v-for` 改为 `v-for="user in projectMembers"`，绑定同一 `id/username` 字段。

- [ ] **Step 4: 把"编辑任务"指派人下拉切换到 projectMembers**

定位 `Board.vue` 中第 187 行 `el-select v-model="editingTask.assignee"`，同上替换 `v-for` 源。

- [ ] **Step 5: Members 邀请/移除后同步 boardStore**

`frontend/src/views/Members.vue` 中：
- `handleInvite` 成功后（`await fetchMembers()` 之后）追加 `await boardStore.fetchProject(projectId.value)`。
- `handleRemoveMember` 成功后（`await fetchMembers()` 之后）追加 `await boardStore.fetchProject(projectId.value)`。

如果 `boardStore` 没有 `fetchProject` 方法，先 `grep -n "fetchProject|currentProject" frontend/src/stores/board/index.ts` 确认；如无，则用 `boardStore.fetchProjects()` 替代（取决于 store 暴露的接口，按实际签名调用）。

- [ ] **Step 6: 手动验证**

1. 项目有 3 个成员 + 1 owner → Board 工具栏的"筛选负责人"下拉只显示 4 人。
2. 在 Members 邀请新成员 → 回到 Board 页面下拉自动出现新人。
3. 在 Board 用"筛选负责人"选某成员 → 看板只剩该成员负责的任务。

- [ ] **Step 7: 提交**

```bash
cd D:/Projects/SyncBoard
git add frontend/src/views/Board.vue frontend/src/views/Members.vue
git commit -m "feat(board): filter assignee by project members; sync store on member change"
```

---

## Task 3: Bug 演示数据 seed

**Files:**
- Create: `backend/bug_tracker/seed.py`
- Create: `backend/bug_tracker/management/__init__.py`
- Create: `backend/bug_tracker/management/commands/__init__.py`
- Create: `backend/bug_tracker/management/commands/seed_bugs_demo.py`
- Create: `backend/tests/test_bug_seed.py`
- Modify: `backend/bug_tracker/views.py`
- Modify: `backend/bug_tracker/urls.py`
- Modify: `backend/room/management/commands/seed_ci_data.py`
- Modify: `frontend/src/api/bug.ts`
- Modify: `frontend/src/views/bug/BugList.vue`

- [ ] **Step 1: 写失败测试 — seed 幂等性**

新建 `backend/tests/test_bug_seed.py`：

```python
"""Bug seed 演示数据 测试"""
import pytest
from django.contrib.auth.models import User
from bug_tracker.models import Bug
from bug_tracker.seed import seed_demo_bugs


@pytest.mark.django_db
class TestSeedDemoBugs:
    def test_first_call_creates_eight(self, test_project):
        added = seed_demo_bugs(test_project)
        assert added == 8
        assert Bug.objects.filter(project=test_project, title__startswith='[DEMO]').count() == 8

    def test_idempotent(self, test_project):
        seed_demo_bugs(test_project)
        added2 = seed_demo_bugs(test_project)
        assert added2 == 0
        assert Bug.objects.filter(project=test_project, title__startswith='[DEMO]').count() == 8

    def test_covers_status_and_severity(self, test_project):
        seed_demo_bugs(test_project)
        statuses = set(Bug.objects.filter(project=test_project, title__startswith='[DEMO]').values_list('status', flat=True))
        assert {'new', 'confirmed', 'fixing', 'closed', 'reopened'}.issubset(statuses)
        severities = set(Bug.objects.filter(project=test_project, title__startswith='[DEMO]').values_list('severity', flat=True))
        assert {'blocker', 'critical', 'major', 'minor'}.issubset(severities)


@pytest.mark.django_db
class TestSeedDemoAPI:
    def test_requires_owner(self, client, test_project, db):
        other = User.objects.create_user('otheruser', password='x')
        client.force_login(other)
        resp = client.post(f'/api/bugs/seed-demo/?project_id={test_project.id}')
        assert resp.status_code == 403

    def test_owner_creates_bugs(self, auth_client, test_project):
        resp = auth_client.post(f'/api/bugs/seed-demo/?project_id={test_project.id}')
        assert resp.status_code == 200
        data = resp.json()
        assert data['added'] == 8
        assert Bug.objects.filter(project=test_project, title__startswith='[DEMO]').count() == 8
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
cd D:/Projects/SyncBoard/backend
python -m pytest tests/test_bug_seed.py -v
```

期望：`ImportError: No module named 'bug_tracker.seed'` 或 `BugSeedDemoView` 路由 404。

- [ ] **Step 3: 新建共享 seed 模块 `backend/bug_tracker/seed.py`**

```python
"""Bug 演示数据 seed — 命令和 API 共用"""
from typing import List
from .models import Bug


# 8 条示例 Bug：覆盖 5 个状态 + 4 个严重度 + 4 个来源
_DEMO_BUGS: List[dict] = [
    {'title': '[DEMO] 登录页 500 错误',         'status': 'new',       'severity': 'blocker',  'source_test_type': 'manual',      'description': '服务端异常'},
    {'title': '[DEMO] 任务列表加载慢',          'status': 'confirmed', 'severity': 'major',    'source_test_type': 'performance', 'description': 'P95 > 2s'},
    {'title': '[DEMO] API 返回字段缺失',        'status': 'assigned',  'severity': 'critical', 'source_test_type': 'api_auto',    'description': 'data 字段为空'},
    {'title': '[DEMO] 邀请成员后未刷新',        'status': 'fixing',    'severity': 'major',    'source_test_type': 'ui_auto',     'description': '需手动刷新'},
    {'title': '[DEMO] 看板拖拽卡顿',            'status': 'fixed',     'severity': 'minor',    'source_test_type': 'ui_auto',     'description': 'GPU 占用高'},
    {'title': '[DEMO] 评论提交失败',            'status': 'verifying', 'severity': 'major',    'source_test_type': 'manual',      'description': '前端 400'},
    {'title': '[DEMO] 项目设置无法保存',        'status': 'closed',    'severity': 'minor',    'source_test_type': 'manual',      'description': '已修复'},
    {'title': '[DEMO] 性能报告 P95 抖动',       'status': 'reopened',  'severity': 'critical', 'source_test_type': 'performance', 'description': '重新出现'},
]

_PRIORITY_BY_STATUS = {
    'new': 'p1', 'confirmed': 'p1', 'assigned': 'p1', 'fixing': 'p2',
    'fixed': 'p3', 'verifying': 'p2', 'closed': 'p3', 'reopened': 'p0',
}


def seed_demo_bugs(project) -> int:
    """为项目创建 8 条 [DEMO] 前缀的 Bug。返回新增条数。幂等。"""
    if Bug.objects.filter(project=project, title__startswith='[DEMO]').exists():
        return 0

    members = list(project.members.all())
    if not members:
        # 没有成员则让 owner 当 assignee
        members = [project.owner]
    owner = project.owner

    created = 0
    for spec in _DEMO_BUGS:
        Bug.objects.create(
            project=project,
            title=spec['title'],
            description=spec['description'],
            status=spec['status'],
            severity=spec['severity'],
            priority=_PRIORITY_BY_STATUS[spec['status']],
            source_test_type=spec['source_test_type'],
            reporter=owner,
            assignee=members[created % len(members)],
        )
        created += 1
    return created
```

- [ ] **Step 4: 新建 management 包**

```bash
mkdir -p D:/Projects/SyncBoard/backend/bug_tracker/management/commands
```

`backend/bug_tracker/management/__init__.py`：空文件（touch）。
`backend/bug_tracker/management/commands/__init__.py`：空文件（touch）。

- [ ] **Step 5: 新建 command `backend/bug_tracker/management/commands/seed_bugs_demo.py`**

```python
"""为指定项目种入 Bug 演示数据（幂等）"""
from django.core.management.base import BaseCommand
from room.models import Project
from bug_tracker.seed import seed_demo_bugs


class Command(BaseCommand):
    help = '为项目种入 8 条 [DEMO] 前缀的 Bug（幂等）'

    def add_arguments(self, parser):
        parser.add_argument(
            '--project', dest='project_id', default=None,
            help='项目 ID（不传则取名为 "CI Automantion Project" 的项目）',
        )

    def handle(self, *args, **options):
        project_id = options.get('project_id')
        if project_id:
            project = Project.objects.filter(pk=project_id).first()
        else:
            project = Project.objects.filter(name='CI Automantion Project').first()

        if not project:
            self.stdout.write(self.style.WARNING('未找到目标项目，跳过'))
            return

        added = seed_demo_bugs(project)
        if added:
            self.stdout.write(self.style.SUCCESS(f'✅ 已生成 {added} 条示例 Bug'))
        else:
            self.stdout.write('ℹ️ 示例 Bug 已存在，跳过')
```

- [ ] **Step 6: 在 `bug_tracker/views.py` 末尾新增 `BugSeedDemoView`**

在 `bug_tracker/views.py` 文件末尾追加：

```python
from django.shortcuts import get_object_or_404
from .seed import seed_demo_bugs


class BugSeedDemoView(APIView):
    """POST /api/bugs/seed-demo/?project_id=X  — 仅 owner 允许"""
    permission_classes = [IsAuthenticated]

    def post(self, request):
        from room.models import Project
        project_id = request.query_params.get('project_id')
        if not project_id:
            return Response({'error': '缺少 project_id'}, status=status.HTTP_400_BAD_REQUEST)
        project = get_object_or_404(Project, pk=project_id)
        if project.owner != request.user:
            return Response({'detail': '只有项目负责人可以导入示例 Bug'}, status=status.HTTP_403_FORBIDDEN)
        added = seed_demo_bugs(project)
        return Response({'added': added, 'total': project.bugs.count()})
```

注意：`status` 在原 views.py 已 import，无需重复。

- [ ] **Step 7: 在 `backend/bug_tracker/urls.py` 注册路由**

替换 `backend/bug_tracker/urls.py` 全文为：

```python
from django.urls import include, path
from rest_framework.routers import DefaultRouter

from .views import BugStatsView, BugViewSet, BugSeedDemoView

router = DefaultRouter()
router.register(r'bugs', BugViewSet, basename='bug')

urlpatterns = [
    path('bugs/stats/', BugStatsView.as_view(), name='bug_stats'),
    path('bugs/seed-demo/', BugSeedDemoView.as_view(), name='bug_seed_demo'),
    path('', include(router.urls)),
]
```

- [ ] **Step 8: 串接 CI seed**

编辑 `backend/room/management/commands/seed_ci_data.py`，在 `handle` 方法末尾（`self.stdout.write(self.style.SUCCESS('✨ 所有测试数据准备就绪！'))` 之前）追加：

```python
        # 4. 串接 Bug 演示数据 seed（幂等）
        from django.core.management import call_command
        call_command('seed_bugs_demo')
```

- [ ] **Step 9: 运行测试，确认通过**

```bash
cd D:/Projects/SyncBoard/backend
python -m pytest tests/test_bug_seed.py -v
```

期望：5 个 test 全部 PASS（首次创建 8 条、第二次幂等返回 0、状态/严重度覆盖、API 403、API 200）。

- [ ] **Step 10: 提交后端改动**

```bash
cd D:/Projects/SyncBoard
git add backend/bug_tracker/seed.py \
        backend/bug_tracker/management/__init__.py \
        backend/bug_tracker/management/commands/__init__.py \
        backend/bug_tracker/management/commands/seed_bugs_demo.py \
        backend/bug_tracker/views.py \
        backend/bug_tracker/urls.py \
        backend/room/management/commands/seed_ci_data.py \
        backend/tests/test_bug_seed.py
git commit -m "feat(bugs): add seed_demo_bugs command + API endpoint; wire into CI seed"
```

- [ ] **Step 11: 在 `frontend/src/api/bug.ts` 末尾新增 `seedDemoBugs`**

在 `bug.ts` 文件最后追加：

```typescript
export const seedDemoBugs = (projectId: string): Promise<{ added: number; total: number }> =>
  service.post(`/bugs/seed-demo/`, null, { params: { project_id: projectId } }) as Promise<{ added: number; total: number }>;
```

- [ ] **Step 12: 在 `BugList.vue` 空态加 owner-only 按钮**

定位 `frontend/src/views/bug/BugList.vue`：
- 在 `<script setup>` 顶部 import 区域追加：
  ```typescript
  import { useBoardStore } from '@/stores/board';
  import { useAuthStore } from '@/stores/Auth';
  import { seedDemoBugs } from '@/api/bug';
  ```
- 新增 store 引用：
  ```typescript
  const boardStore = useBoardStore();
  const authStore = useAuthStore();
  ```
- 新增计算属性：
  ```typescript
  const isOwner = computed(() =>
    boardStore.currentProject?.owner_details?.id === authStore.user?.id
  );
  ```
- 新增方法（紧跟 `loadStats` 之后）：
  ```typescript
  const handleSeedDemo = async () => {
    try {
      const res = await seedDemoBugs(projectId);
      ElMessage.success(`已导入 ${res.added} 条示例 Bug`);
      await loadList();
      await loadStats();
    } catch (e: any) {
      ElMessage.error(e?.response?.data?.detail || '导入失败');
    }
  };
  ```
- 在 `<el-table :data="bugs" v-loading="loading" ...>` 之前插入空态提示：
  ```vue
  <div v-if="!loading && bugs.length === 0 && isOwner" class="empty-demo-banner">
    <span>暂无 Bug 数据</span>
    <el-button size="small" type="primary" @click="handleSeedDemo">导入示例 Bug</el-button>
  </div>
  ```
- 在 `<style scoped>` 块末尾追加：
  ```css
  .empty-demo-banner {
    display: flex; align-items: center; justify-content: space-between;
    padding: 12px 16px; background: var(--el-fill-color-light);
    margin-bottom: 12px; border: 1px dashed var(--el-border-color);
  }
  ```

- [ ] **Step 13: 手动验证**

1. 在干净项目下打开 `/projects/<id>/bugs/` → 表格空 + 显示"导入示例 Bug"按钮。
2. 点击按钮 → 表格出现 8 条 [DEMO] Bug。
3. 再次点击 → 提示"已导入 0 条"或不出现重复。
4. 用非 owner 账号访问 → 不显示按钮。

- [ ] **Step 14: 提交前端改动**

```bash
cd D:/Projects/SyncBoard
git add frontend/src/api/bug.ts frontend/src/views/bug/BugList.vue
git commit -m "feat(bugs): owner-only 'import demo' button on empty bug list"
```

---

## Task 4: 质量报告 Bug 密度改用 `bug_tracker.Bug`

**Files:**
- Modify: `backend/qa_center/views_devops.py:855-861`
- Create: `backend/tests/test_quality_report.py`

- [ ] **Step 1: 写失败测试**

新建 `backend/tests/test_quality_report.py`：

```python
"""质量报告端点测试"""
import pytest
from django.contrib.auth.models import User
from bug_tracker.models import Bug


@pytest.mark.django_db
class TestQualityReportBugDensity:
    def test_uses_bug_model_not_task_tag(self, auth_client, test_project, test_user):
        # 给项目创建一个 Bug
        Bug.objects.create(
            project=test_project, title='T1', reporter=test_user,
            severity='major', priority='p2', status='new',
        )
        # 给项目造一个名为 'bug' 的 Tag 但不挂 task —— 不应影响 bug_count
        from room.models import Tag
        Tag.objects.create(project=test_project, name='bug', color='#f00')

        resp = auth_client.get(f'/api/qa/devops/quality-report/?project_id={test_project.id}')
        assert resp.status_code == 200
        data = resp.json()
        # summary.bug_count 必须来自 bug_tracker.Bug
        assert data['summary']['bug_count'] == 1
        # dimensions 中 Bug密度 的 detail 文本应反映 1 个 Bug
        bug_dim = next(d for d in data['dimensions'] if d['name'] == 'Bug密度')
        assert '1 个 Bug' in bug_dim['detail']

    def test_zero_bugs_zero_density(self, auth_client, test_project):
        resp = auth_client.get(f'/api/qa/devops/quality-report/?project_id={test_project.id}')
        assert resp.status_code == 200
        data = resp.json()
        assert data['summary']['bug_count'] == 0
        bug_dim = next(d for d in data['dimensions'] if d['name'] == 'Bug密度')
        assert bug_dim['score'] == 20  # 0% 密度 → 满分
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
cd D:/Projects/SyncBoard/backend
python -m pytest tests/test_quality_report.py -v
```

期望：FAIL，因为 `summary.bug_count` 反映的是 Tag 计数（Tag 数 = 1）。

- [ ] **Step 3: 修改 `backend/qa_center/views_devops.py`**

把 855-861 行的"4. Bug 密度"整段替换为：

```python
        # 4. Bug 密度：基于 bug_tracker.Bug 模型统计
        from bug_tracker.models import Bug
        bug_count = Bug.objects.filter(project_id=project_id).count()
        bug_density = round(bug_count / total_tasks * 100, 1) if total_tasks > 0 else 0
        bug_score = 20 if bug_density < 10 else (15 if bug_density < 20 else (10 if bug_density < 30 else 5))
```

把 898 行的 `{'name': 'Bug密度', 'score': bug_score, 'max': 20, 'detail': f'{bug_density}%'}` 替换为：

```python
                {'name': 'Bug密度', 'score': bug_score, 'max': 20, 'detail': f'{bug_count} 个 Bug / {total_tasks} 任务 ({bug_density}%)'},
```

定位 `views_devops.py` 901-915 行 `summary` 字典的 `bug_count` 字段，改为：

```python
            'bug_count': bug_count,
```

（如果原本没有此字段则添加。）

- [ ] **Step 4: 运行测试，确认通过**

```bash
cd D:/Projects/SyncBoard/backend
python -m pytest tests/test_quality_report.py -v
```

期望：2 个 test 全部 PASS。

- [ ] **Step 5: 跑全套 backend 测试确保无回归**

```bash
cd D:/Projects/SyncBoard/backend
python -m pytest -q
```

期望：所有原有用例 + 7 个新用例全部 PASS。

- [ ] **Step 6: 提交**

```bash
cd D:/Projects/SyncBoard
git add backend/qa_center/views_devops.py backend/tests/test_quality_report.py
git commit -m "fix(qa): quality report uses bug_tracker.Bug for density & summary"
```

---

## Self-Review

- [x] **Spec 覆盖**：
  - 3.1 Members 改角色 → Task 1
  - 3.2 Board 筛选 → Task 2
  - 3.3 Bug 演示数据 seed（4 个子文件）→ Task 3
  - 3.4 质量报告 Bug 密度 → Task 4
- [x] **无占位符**：所有步骤都给了实际代码与命令
- [x] **类型一致**：`seed_demo_bugs` 在 Task 3 Step 3 定义、Step 1 测试引用、Step 6 视图调用一致；`BugSeedDemoView` 路由在 Step 7 注册；前端 `seedDemoBugs` 在 Step 11 定义、Step 12 调用
- [x] **commit 粒度**：6 次 commit（task 1 / task 2 / task 3 后端 / task 3 前端 / task 4），每 commit 都能独立回滚
- [x] **TDD 覆盖**：Task 3 与 Task 4 都先写测试再写实现
